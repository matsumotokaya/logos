# ベータローンチ準備計画(マイルストーン正本)

最終更新: 2026-07-17

このドキュメントは**ベータ公開までに必要な作業の唯一の正本**。機能ロードマップ(Labs等)は各正本に従い、ここでは「出せる状態にする」ための不足だけを扱う。進捗はこのファイルのチェックボックスを直接更新する。

## 現状診断(2026-07-17時点)

| 領域 | 状態 |
|---|---|
| プロダクト機能 | PoCとして一通り動作(生成プレゼン・Brand Manager・asset catalog・Labs) |
| セキュリティ | **未考慮**。`/api/generate` は認証・レート制限なしで外部AI APIを実行可能。APIルート12本中、認証チェックがあるのは3本のみ。middleware なし |
| 課金 | ゼロ(プラン設計・Stripe・クォータいずれもなし) |
| 法務 | ゼロ(利用規約・プライバシーポリシーなし)。LPの「ロゴは外部送信されない」文言がシーン10のGemini生成と矛盾 |
| 会員ライフサイクル | 登録・匿名昇格は実装済み。**退会・アカウント削除・データ削除は未実装**(settingsはプレースホルダー)。OAuthは配線のみ |
| テスト/CI | ゼロ(`lint` のみ。テストフレームワーク未導入、GitHub Actions なし) |
| 運用 | エラーモニタリング・アナリティクス・バックアップ方針なし |

## 前提となる意思決定(M0)

以下は実装より先に決める。**推奨**を併記する。

- [x] **ベータの形態**: **無料・招待/ウェイトリスト制のクローズドベータ**に決定(2026-07-17)。課金はベータ中に設計し、正式版で開始(特商法表記も課金開始時でよい)
- [x] **ゲスト(匿名)にシーン10生成を許可するか**: **登録ユーザーのみ+回数上限**に決定(2026-07-17)。匿名はコードベースのモックアップまで
- [ ] **ベータで隠す機能**: 推奨 = 発注UI(ダミー)は「Coming soon」化、`/labs` は本番では所有者アカウント限定
- [ ] **サービス名・ドメインの確定**(`lib/config.ts` の `SERVICE_NAME` で一元反映)

---

## M1 — セキュリティ最低ライン 🔴 最優先・着手可能

公開した瞬間に事故る穴を塞ぐ。機能追加より先。

- [x] `/api/generate` に認証必須化+ユーザー単位の生成クォータ(コスト踏み台防止)— 2026-07-17実装。登録ユーザーのみ・24時間で `GENERATION_DAILY_LIMIT`(既定20)回。**要: `0010_generation_quota.sql` の本番適用**
- [x] 全APIルート(12本)の認証・認可監査 — 2026-07-17実施。結果:
  - [x] `/api/mockups/.../[slot]` POST・`/api/mockups/...` DELETE: **R2書き込み/削除がRLS認可より先に走る脆弱性を修正**(DB操作をRLSで認可→成功後にR2副作用の順へ。POSTはcandidate↔logoの対応チェックとサイズ上限も追加)
  - [x] `/api/labs/*` のlab専用6本(generative全4本+workflow jobs/templates): 本番では `LABS_ENABLED=1` を設定しない限り404(`lib/labs-access.ts`)。`/labs` ページ群も同じゲート(`app/labs/layout.tsx`)
  - [x] `/api/labs/workflow/compose`・`/api/labs/workflow/runs` はプレゼン本編が使うため**ゲート対象外**(runsは認証済み。composeは下記の残課題)
  - [x] `/api/presentation-assets`: 公開カタログ(テンプレメタデータのみ)として公開のままで問題なし
- [ ] **残課題: `GET /api/mockups/.../[slot]`(画像配信)が認証なし** — `<img src>` がAuthorizationヘッダーを送れないための構造。非公開ロゴのモックアップ画像がID知識だけで取得可能。署名付きURL(R2 presigned / 期限付きトークン)への移行を設計する
- [x] **`/api/labs/workflow/compose` のバースト制限** — 2026-07-17実装。IP単位60回/分のプロセス内リミッタ(`lib/rate-limit.ts`)。公開プレゼンの描画に必要なため認証は課さない
- [x] **`/api/generate` クォータの並列競合修正** — count→insertの順だと並列リクエストが全て通過できたため、insert→自分の行を含めてcountの順に変更(2026-07-17)
- [ ] **本格的なレート制限**: プロセス内リミッタはサーバーレスではインスタンス単位でしか効かない。本番デプロイ時に Vercel WAF または Upstash Ratelimit を導入する(M6のVercel整備と同時でよい)
- [ ] Supabase RLS 監査: `get_advisors`(security)を実行し全指摘を解消 — **2026-07-17時点でMCPトークン(`SUPABASE_ACCESS_TOKEN_LOGOS`)未設定のため実行不可。トークン設定後のセッションで実施**
- [ ] 既知課題の解消: 組織ロゴ visibility 変更の admin 限定をサーバー側(RPC or trigger)で強制
- [ ] APIリクエストボディの入力バリデーション(サイズ上限・型検証。特に base64 画像とSVG)
- [ ] SVGサニタイズの確認(アップロードSVGの `<script>`/外部参照の無害化。公開ページで他人のSVGを表示するため XSS 経路になる)
- [ ] R2 バケットのアクセス制御・CORS 確認
- [ ] `npm audit` と依存更新、`.env.local`/シークレットの棚卸し(リポジトリ混入チェック)

## M2 — 会員ライフサイクルの完成

入口(登録)はあるが出口(削除)がない状態を解消する。法務(M3)の前提でもある。

- [ ] **退会・アカウント削除**: `/settings` のプレースホルダーを実装。auth.users / public.users / 所有ロゴ・プレゼン・mockups・R2 オブジェクトまで削除(組織所有ロゴの扱いは要設計: 移管 or 残置)
- [ ] **ロゴ削除のカスケード確認**: `logo_mockups`・R2 成果物・`logo_asset_runs` が孤児にならないこと
- [ ] 匿名ユーザーのゴミ掃除方針(一定期間で昇格しなかった匿名データの削除)
- [ ] Google OAuth の有効化(最優先プロバイダ。Apple/Figma はポストベータ可)
- [ ] メールフロー確認: Confirm email の方針決定・パスワードリセット動作確認・カスタムSMTP(組み込みSMTPはレート制限が厳しい)
- [ ] プロフィール編集(表示名程度)の実装

## M3 — 法務・信頼

- [ ] **プライバシーポリシー**(`/privacy`): 取得データ(メール・ロゴ・生成物)、第三者提供(Google Gemini・Together/Recraft・Cloudflare・Supabase・Vercel)、削除請求の方法
- [ ] **利用規約**(`/terms`): アップロードコンテンツの権利(ユーザーが権利を持つロゴのみ)、商標の免責、公開ロゴ図鑑への掲載条件、ベータの無保証
- [ ] **LP文言の実態整合**: 「解析はブラウザ内で完結」は解析シーンに限定した表現に修正し、シーン10は「生成時にサーバー経由でAI APIへ送信」と明示(現状は事実と異なる表示)
- [ ] フッターから法務ページへのリンク(5言語対応は ja/en 先行でよい)
- [ ] 公開ロゴのテイクダウン導線(問い合わせ先の明示。商標権者からの申立て対応)
- [ ] 特商法表記(**課金開始時に必須**。クローズドベータ中は不要)
- [ ] Cookie/アナリティクス方針(M6のツール選定と同時)

## M4 — 課金設計+Stripe

ベータ中に設計・実装し、正式版で課金開始する想定(M0の決定に従う)。

- [ ] **プラン設計**: Free / Pro の線引き(候補: ロゴ数、AI生成回数/月、公開ページのブランディング、組織メンバー数、CDN配信)
- [ ] entitlements の持ち方を設計(`orgs`/`users` に plan 列 + クォータテーブル。RLS と `BrandRepo` に組み込む)
- [ ] Stripe 導入: Checkout(サブスク)+ Customer Portal + Webhook(`checkout.session.completed` / `customer.subscription.*`)
- [ ] クォータ実装をM1の生成回数制限と統合(無料枠→プラン枠へ拡張)
- [ ] 請求まわりのUI(Brand Manager にプラン表示・アップグレード導線)
- [ ] テスト環境(Stripe test mode)での一連の動作確認

## M5 — コード品質・リファクタリング

「成り行きで作った」状態からベータ運用に耐える最低限へ。全面書き直しはしない。

- [ ] テスト基盤導入(Vitest 推奨)。まず純粋ロジックから: `lib/svg.ts` / `lib/paths.ts` / `lib/color.ts`
- [ ] CI(GitHub Actions): lint + typecheck + test + build を PR 必須に
- [ ] APIルートの共通化: 認証取得・エラーレスポンス・バリデーションのヘルパーを1箇所に(M1の実装と同時に行うと二度手間がない)
- [ ] `lib/store/supabase.ts`(900行超)の責務分割(logo / org / presentation / registry)
- [ ] `console.log` 等デバッグコードの一掃、TypeScript strict 状態の確認
- [ ] 旧ルート(`/lab`、`/labs/image`、`/brand/logos/[id]`)のリダイレクト整理

## M6 — 運用体制

- [ ] エラーモニタリング(Sentry)導入: フロント+APIルート
- [ ] アナリティクス導入(プライバシー配慮型: Plausible / Vercel Analytics 等)+ 主要ファネル(アップロード→プレゼン閲覧→登録)
- [ ] Vercel 本番プロジェクト整備: 環境変数一式・プレビュー環境と本番の分離・独自ドメイン
- [ ] Supabase バックアップ方針(PITR の確認)・R2 のライフサイクル方針
- [ ] SEO/メタ: OGP(プレゼン共有時のカード)、`robots.txt`、`/labs` の noindex を実効化、404/エラーページ
- [ ] 運用時のコスト監視(Gemini/Together/Recraft の使用量アラート)

## M7 — ベータ公開準備

- [ ] PoC制約の明示: SVGのみ対応をUI上で正直に表示(PNG対応はポストベータのロードマップとして掲示)
- [ ] 発注UI(ダミー)の「Coming soon」化 or フィーチャーフラグで非表示
- [ ] 招待制/ウェイトリストの仕組み(M0の決定に従う)
- [ ] オンボーディング動線の見直し(初回アップロード→驚き、までの摩擦除去)
- [ ] フィードバック導線(フォーム or メール)
- [ ] サンプルプレゼン(`/p/sample`)の最終品質チェック
- [ ] 全ページの5言語表示くずれ・モバイル表示の通し確認

---

## 進め方

推奨順序: **M0(決定)→ M1 → M2 → M3 →(M5をM1〜と並行)→ M6 → M7 で無料ベータ公開 → M4(課金)で正式版**。

- M1 と M5 の APIヘルパー共通化は同時に着手すると効率がよい
- M3 の LP文言修正(実態との矛盾)だけは軽作業なので M1 と同時に済ませる
- M4(Stripe)はベータ公開のブロッカーにしない(クローズドベータ前提の場合)

## ポストベータ(ここでは扱わない)

PNG/AI入力対応、発注の実オペ連携、ロゴCDN配信(R2+Workers)、Apple/Figma OAuth、個人ハンドル、図鑑の検索・タグ、ランタイムassetの永続ワーカー化([docs/logo-entity-lab-integration.md](logo-entity-lab-integration.md))。
