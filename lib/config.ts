// Service identity — the name is provisional. Change it here and it updates everywhere.
export const SERVICE_NAME = "logos";
// The descriptor that always rides with the wordmark. It exists because the
// name alone reads as "a tool that makes logos" — the opposite of what this
// is. See docs/product.md §12.
export const SERVICE_TAGLINE = "Brand System of Record for Go-to-Market";

// The R&D labs run under their own wordmark: same header, "labos" mode.
// One character off from the product name on purpose — the labs are the
// product's workshop, not a separate service.
export const LABS_NAME = "labos";
