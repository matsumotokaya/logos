# HANDOFF — 最初に読む1枚

最終更新: 2026-08-29

> **この文書の書き方**(次のセッションが守ること)
>
> 1. **80行を超えてはいけない。** 超えたら足すのではなく、下の3択へ振り分ける
> 2. **追記しない。上書きする。** 日誌にすると現在地が読めなくなる(READMEの「現在地」が実際にそうなった)
> 3. **溢れたものの行き先は3つだけ**——本物のタスクは [README](README.md) の「残タスク」へ / 済んだ経緯は [docs/old/](docs/old/README.md) へ / **どちらでもないものは削除する**
> 4. **位置づけと成熟度を同じ文に書かない。** 「これが正式版」と「これは動くことが確かめてある」は別の主張で、1文に入れると必ず取り違えられる(2026-08-23 に実際に起きた)
> 5. **READMEは索引。通読させるものではない。** ここから名指しした場所だけを開く

## 現在地

**event-cm × `sumi`(モダンジャパニーズ)は納品水準を通った唯一のテンプレート**(2026-08-24)。

**event-cm × `standard`(スタンダード)は 2026-08-27 に素材が入り、9シーン全部が写真で埋まった。** 追加ダイアログの「イベント紹介動画 › スタイル」がアートディレクション(モダンジャパニーズ / スタンダード)で、BGM・写真・見本写真・数字(算用 `01 02 03`)・効果音の列が塗りに従う。以前この位置にあった「スタンダード」は**廃止決定済みの event-promo**(読み上げなし)を作っていた——ダイアログから外した(既存Takeはそのまま)。

**エンドカードが動画の上に立つようになった**(`theme.endCard`)。**墨の側も落ちていた**——承認済みのラボ映画は富士が雲海に隠れる映像の上でマークを見せていたのに、持ち帰りが `logoOut` の地を落としていた(Phase B5)。墨はラボの数字そのまま(`rgba(8,6,4,0.58)`)、スタンダードは支給された都市の映像を逆向きに(明るく洗う)。**両方とも仮素材で `licensed: false`**——プレビュー限定で、書き出しからは外れる(除外の経路はこの日に初めて実装した)。**ロイヤリティフリーは再配布許諾ではない**ので生成物に差し替える(2026-08-28)。

**2026-08-26 のレビューで直したもの**: プレイヤーの黒い角丸シェル(動画の一部に見える装飾)を撤去 / 効果音の初版2本が1秒後も鳴るジングルだったのを、承認済みの和の列の実測エンベロープに収まる音へ差し替え(規則はテスト化) / BGMを**その曲のピーク余裕いっぱい**まで増音(基準 −16 dBFS は静かな方の曲の限界を全曲に課していた) / 薄いイントロを `startFromSec` でスキップし、**トリムした曲を `<Loop>` で明示的に回す**(`loop`+`trimBefore` はプレイヤーでループせず、画面26秒無音・MP4は最後まで、という食い違いが出ていた) / **長い処理にトーストを付けた**——run card は資料系ステージにしか無く、資料の無いTakeで「動画を作り直す」を押すと3つのLLM/TTS工程が無言で走っていた。

**Factory シリーズ(別プロジェクト)への移植で見つかった指摘のうち5件を取り込んだ**(2026-08-29)。TTSが429の本文に書かれた待ち時間を無視していた件、シードの原稿が「LLMが書いた」と嘘をついていた件、`rendererRevision` を誰も読んでおらず**描画を直しても既存MP4が黙って古いままだった**件、文字数予算に**検査する側が無かった**件、モデル名リテラルが4箇所あった件。取り込まなかったものと理由、残り1件は [README「Factory シリーズからの持ち帰り」](README.md)。

**いま入っているデータは全部サンプル。** 既存Takeの整合やバックフィルは**気にしない**。

## 次の一手

| 順 | 対象 | 何をするか |
| --- | --- | --- |
| 1 | **エンドカードの地を生成物に差し替える**(依頼主) | 支給素材はロイヤリティフリーだが**再配布の可否が未確認**なので仮のまま(2026-08-28 判断)。プロンプトは [docs/demo-assets.md §6](docs/demo-assets.md)——墨は雲海、スタンダードは雲と光。**まず静止画で見る**(締めは4秒なので静止画でも成立する)。差し替え後に `licensed: true` + コミット |
| 2 | **スタンダードを通しで見る** | `npm run event-cm:walkthrough`(課金あり)。静止画は合格したので、次に見るのは**尺・読み上げ・音のバランス**。`themes:compare` は静止画までしか答えない |
| 3 | **製品のスタンダード**(product-cm × `standard`) | `cm_script` を `narration/voice/字幕` の3語へ。`brief.narrator` の改名とセット |

**LLMは積極的に使う**(方針は [AGENTS.md](AGENTS.md))。**コストを理由に品質を落とす判断はこちらでしない。**

### 直近の小さい残り

- **コーポレートの効果音の音色は未試聴**(音量と尾はテストで承認帯に固定済み・READMEの「音の列」)。`piano-single1` / `title1` / `sceneswitch2` / `spotlight1`。**却下された初版から学んだ規則**: 頭1秒の音量が合っていても、1秒過ぎて鳴り続ける音はジングルであって合図ではない
- **`templates:sync` は未実行**。event-cm の台帳 spec(summary・name・artDirections)と **`rendererRevision`(→ `@2026-08-28`)** が変わったが、リモートの版は全部 `draft` なので drift エラーにはならない。次にリモートへ書くタイミングで `npm run templates:sync`(書き込みなので承認を取る)
- **標準のBGMは `shine-through-tokyo`(3分)に差し替え済み**(2026-08-30)。51秒の映画は曲の終わりに届かないので**継ぎ目が起きない**。製品紹介動画も同じ曲(標準の塗りは全部これ)。`bright-corporate`(40秒)はプールに残る2番目の選択肢。**全曲ピーク制限**なのでこれ以上の増音はリマスター待ち——新曲の実効音量は承認済みの墨とほぼ同じ(−16 dB台)で、フルスケールの `bright` より約4 dB低い。**曲を選ぶUIはまだ無い**(台帳の宣言を変えるだけ)
- **背景写真のUpscale**(依頼主・1枚1クリック)。墨3枚・スタンダード6枚が素の 1456×816 で、Ken Burnsが寄ると約1.43倍になり眠い。同じジョブのUpscaleなら構図は変わらない
- **プロンプトは使い捨て**([docs/demo-assets.md](docs/demo-assets.md) の冒頭)。素材が届いた§は削除する。要件(§5)だけが残る
- **プレビューで効果音だけが大きくBGMが小さい問題は未解決**(5回以上指摘・原因不明)。**書き出したMP4では起きない**(実測 平均 −15.6 / ピーク −1.7 dB、曲そのものと0.1 dB以内)。2026-08-30 に4つの仮説を潰したが**症状は消えていない**。潰した仮説・容疑から外れたもの・次に試す線(**BGMを40秒の曲に替えて消えるか**が最安の実験)は [README「未解決: プレビューで効果音だけが…」](README.md)。**音量はプレビューで決めない**——`--codec=wav` で音声だけ書き出せば数十秒で測れる
- **効果音は本番で鳴らない**(2026-08-30 に判明)。既定素材は**システムの一部としてgitに入れる**方針に変わり、BGM・写真・マークは入った。**効果音だけが例外**——効果音ラボが再配布を禁じており、リポジトリが公開だから。`sfx:sync` はR2へ上げているが**読む実装が無い**。**決めるのは依頼主**: リポジトリを非公開にする(1操作で全部解決)か、63音を自社生成に置き換えるか([docs/demo-assets.md §3](docs/demo-assets.md))

## 信じてはいけないもの

| 書いてある場所 | 何が誤読されるか |
| --- | --- |
| README「**`standard` が正式版で、`sumi` は派生**」 | **方針であって成熟度ではない。** 既定の塗りは 2026-08-30 に standard へ倒した(依頼主判断)が、**納品水準で通しを見たのは `sumi` だけ**。標準は静止画と素材が揃った段階で、尺・読み上げ・音のバランスは未検証(次の一手2) |
| [ASSET-PROMPTS.md](labs/freehand/sake-2026/ASSET-PROMPTS.md) | **要件は正本、題材は正本ではない。** 16:9・片側3〜4割の空き・文字なし・顔なし・セット内で照明族を固定——ここは継ぐ。日本酒の語彙は継がない。**明暗はスタンダードでは逆**(§11) |
| README「残タスク」の各項目 | **バックログであって優先順位ではない。** 次にやることはこの文書の「次の一手」だけ |
| 上の「**`sumi` は納品水準を通った**」 | **その字面はこの Mac のヒラギノ明朝で、どこにも同梱されていない。** 書体はシステムフォント名の羅列だけ(`theme.ts`)で `loadFont` が0件。**日本語フォントの無い Linux では豆腐**になるので、既定路線の Remotion Lambda 移行はこのままでは通らない。**承認済みのラボ映画は Shippori Mincho を同梱していた**——エンドカードの地と同じで、持ち帰りが落とした([README「Factory シリーズからの持ち帰り」](README.md)) |

## 先に開く場所

1. **[docs/demo-assets.md](docs/demo-assets.md)** — §2(塗りごとに別プール・なぜ)・§5(**消えない要件**)・**§6(依頼中=エンドカードの地の差し替え)**
2. **[README「アートディレクションは交換できる」](README.md)** — 作成時の選択・塗りごとの服・残っていること
3. **[labs/freehand/README.md](labs/freehand/README.md) の「持ち帰り計画」** — Phase A〜E で何が済み何が残っているか

[README.md](README.md) は正本の索引として引く。**通読しない。**
